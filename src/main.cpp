#include <string>
#include <iostream>
#include "parser.cpp"

using namespace std;

int main(int argv, char** argc) {

    while (true) {
        cout << "$ ";
        Parser* p = new Parser();
    }

    return 0;
}